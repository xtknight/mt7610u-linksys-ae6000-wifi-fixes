Filename format:
    defs.chipset.<CHIPSET>.mk

Each chipset fragment should define the following Makefile variables:
	WFLAGS_${CHIPSET}-y
	CHIPSET_DAT_${CHIPSET}

Chipset fragments may define the following Makefile variables:
	WFLAGS_${CHIPSET}_${RT28xx_MODE}-y
