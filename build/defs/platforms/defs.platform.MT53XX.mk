# MT53XX platform definitions file
#
# Defines the following variables:
#    LINUX_SRC
#    LINUX_SRC_MODULE
#    CROSS_COMPILE
#    PLATFORM_SUPPORTED (only if known to be supported)
#
# NOTE: In the file set upon which this was based, these variables were
# never defined.

LINUX_SRC ?=
LINUX_SRC_MODULE ?=
CROSS_COMPILE ?=
